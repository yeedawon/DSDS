# Python
__pycache__/
*.pyc

# Virtual environment (~1.2 GB)
nli_env/

# Large / regenerable artifacts
# emb/   = SBERT embeddings (~539 MB, incl. 293 MB train_all_X.npy); regenerate from the notebook
# preds/ = cached prediction vectors
emb/
preds/

# Superseded / non-canonical experiment artifacts (kept locally, not published)
# results.json            = old whole-validation "6-cell" results (no BERT); design the paper avoids
# results_significance.json + significance_experiment.py = separate N-sweep; paper stats come from results_augmentation.json
results.json
results_significance.json
significance_experiment.py

# OS
.DS_Store
